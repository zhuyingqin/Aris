interface:
  display_name: "OpenAlex Search"
  short_description: "Export OpenAlex titles and abstracts."
  aris_runtime_note: 'Resolve OPENALEX_EXPORT per SKILL.md Export Script; invoke OpenAlex only via python "$OPENALEX_EXPORT" so bundled, exported, and repo-local installs all use the same CLI.'
  default_prompt: "Use OpenAlex Search to design reproducible OpenAlex works queries and download titles, abstracts, and metadata for every query expression."
