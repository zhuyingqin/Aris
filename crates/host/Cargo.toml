[package]
name = "somni-host"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Host-independent Executor/Reviewer state machine for Somni surfaces"

[dependencies]
runtime = { path = "../runtime" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[lints]
workspace = true

[dev-dependencies]
tempfile = "3.27"
